# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# AI Agent
langgraph==0.2.35
langchain==0.3.1
langchain-groq==0.2.0
langchain-community==0.3.1
groq==0.11.0

# MCP
fastmcp==0.4.1

# Database — PostgreSQL
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0
alembic==1.13.3
psycopg2-binary==2.9.9          # для Alembic CLI (sync)

# LangGraph persistence (PostgresSaver)
langgraph-checkpoint-postgres==2.0.3

# Vector DB — Qdrant
qdrant-client==1.11.3

# Embeddings
# sentence-transformers==3.1.1
# torch==2.4.1                    # CPU; замени на torch+cu118 если есть GPU

# Reranking (cross-encoder)
# используется sentence-transformers CrossEncoder — уже включён выше

# Auth — JWT + bcrypt
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Scheduler
apscheduler==3.10.4

# Korean Stock Market Data
pykrx==1.0.47
finance-datareader==0.9.50
requests==2.32.3                # для DART API (REST)

# Technical Indicators
pandas==2.2.3
numpy>=1.26.0
# pandas-ta>=0.4.67b0          # RSI, MACD, MA и другие индикаторы

# Config & Validation
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# RAGAS — RAG evaluation
#ragas==0.2.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2                   # async test client для FastAPI
pytest-cov==5.0.0               # coverage

# Email validator
email-validator==2.2.0